class Only(Base):  # R1112
    """
    ::

        <only> = <generic-spec>
                 | <only-use-name>
                 | <rename>

    """

    subclass_names = ["Generic_Spec", "Only_Use_Name", "Rename"]
